#!/usr/bin/env bash

set -euxo pipefail

if [ "$#" -ne 1 ]; then
  echo "Usage: $0 <release_version>"
  exit 1
fi

mkdir -p snap/gui

export DO_STARTUP_NOTIFY="true"
export APP_NAME="Gram"
export APP_CLI="gram"
export APP_ICON="\${SNAP}/meta/gui/gram.png"
export APP_ARGS="%U"
envsubst < "crates/gram/resources/gram.desktop.in" > "snap/gui/gram.desktop"
cp "crates/gram/resources/app-icon.png" "snap/gui/gram.png"

RELEASE_VERSION="$1" envsubst < crates/gram/resources/snap/snapcraft.yaml.in > snap/snapcraft.yaml

# Clean seems to be needed to actually check that the snapcraft.yaml
# works.  For example, when a `stage-package` is removed, it will
# still remain on rebuild.
snapcraft clean

snapcraft
