[package]
name = "odin"
version = "0.3.1"
edition = "2021"
license = "MIT"

[lib]
path = "src/odin.rs"
crate-type = ["cdylib"]

[dependencies]
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
tehanu_extension_api = { path = "../../crates/extension_api" }
